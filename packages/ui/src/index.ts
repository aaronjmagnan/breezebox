/**
 * @breezebox/ui
 *
 * The shared design system (backbone §6). Every tool imports these components
 * rather than restyling from scratch, which is what keeps the "same OS feel"
 * across tools.
 *
 * Constraints every component here must hold to:
 *   - works from a 360px phone up to a laptop
 *   - tap targets at least 44px
 *   - no hover-only interactions
 *   - two font weights only
 *   - the four accents are used sparingly, never as a background
 *
 * Components land in step 5: Button, Card, Tile, AppHeader.
 */

export {};
